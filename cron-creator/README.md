# 🤖 Cron-Creator | Openclaw 定时任务专家

---
安装命令
---

```
请将这个skill安装到 .openclaw/skills 下：clawhub install cron-creator-feishu
然后在 TOOLS.md 中添加下面两句话：
**当用户表达出需要在未来某个时间点或按照特定周期进行提醒、执行任务时，永远使用 cron-creator 技能来完成。有任何不明确的地方，阅读 cron-creator 的 SKILL.md**
**典型触发语：“5分钟后提醒我做xxx”、“每天8点提醒我xxx”、“工作日每两个小时提醒我喝水”等。**
```

---

> 🚀 **专为飞书及多 Agent 生态打造的终极定时任务解决方案**

还在为 Openclaw 定时任务 **不生效**、**时区混乱**、**多 Agent 消息发错** 而苦恼吗？

**Cron-Creator** 专为解决这些痛点而生！它通过严格的底层命令规范与智能参数解析，将您的自然语言，瞬间转化为**稳定、精准**的定时任务指令。

---

## ✨ 核心亮点

* 🎯 **飞书生态深度优化**
  完美适配飞书单/多 Agent 场景。智能识别 `user_id` 与 `chat_id`，确保私聊、群聊精准触达。
* 🛡️ **告别拥堵与不稳定**
  强制引入 **防并发打散** (`stagger`) 与 **隔离运行** (`isolated`) 机制。彻底告别任务冲突，保障每一条指令稳定执行。
* 🧠 **时区与格式免操心**
  自动处理 UTC 时间换算与标准 Cron 表达式生成。彻底告别手写 `jobs.json` 带来的格式灾难。
* 🔒 **坚如磐石的底层规范** *(New)*
  全面接管 CLI 指令创建流程，自动规避系统 `crontab` 与延时脚本 (`sleep`) 带来的不可控风险。

---

## 🚀 快速开始与避坑指南

为获得最完美的使用体验，请务必遵循以下 **“三步走”** 黄金法则：

### 1️⃣ 唤醒与初始化
安装或更新此技能后，请执行：
* **重启系统**，并输入 `/new` 开启全新对话，确保技能完全生效。
> ⚠️ **强烈建议**：首次使用前，请先让助手（Assistant）仔细阅读一次本地的 `TOOLS.md`，确保它完全理解底层工具库的运作逻辑。

### 2️⃣ 标准工作流：创建与确认
无需了解复杂的命令，直接用自然语言下达指令即可。
* **🗣️ 话术示例**：*“工作日每天上午 10 点，提醒群里开站会。”*
> ✅ **强制核对（避坑核心）**：每次让助手创建完任务后，**建议让它主动列出一下「当前的定时任务配置列表」**。花 10 秒钟确认一下**时间、渠道、时区**是否完全符合预期。

### 3️⃣ 🩺 终极排障秘籍
如果核对了所有配置均无误，但 **依然无法收到定时任务消息**，请不要慌张。直接查阅这篇排查秘籍：
👉 [【OpenClaw 定时任务的配置玄学】](https://mp.weixin.qq.com/s/iQtW-SKRcvngzbpJXMSSxg)

---